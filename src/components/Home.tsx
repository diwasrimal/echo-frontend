// import useAuth from "@/hooks/useAuth";

export default function Home() {
  // const { checking, loggedIn, userId } = useAuth();
  // console.log(
  //   `AuthContext while loading home: checking: ${checking}, loggedIn: ${loggedIn}, userId: ${userId}, jwt: ${jwt}`,
  // );
  return "HOME";
}
